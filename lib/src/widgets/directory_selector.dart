import 'package:flutter/material.dart';
    
class DirectorySelector extends StatelessWidget {

  const DirectorySelector({ Key? key }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}